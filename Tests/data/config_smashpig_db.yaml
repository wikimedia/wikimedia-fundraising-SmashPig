default:
    data-store:
        pending-db:
            class: PDO
            inst-args:
                - 'sqlite::memory:'

        damaged-db:
            class: PDO
            inst-args:
                - 'sqlite::memory:'
