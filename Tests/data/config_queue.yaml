default:
    data-store:
        test:
            class: PHPQueue\Backend\PDO
            inst-args:
                -
                    connection_string: 'sqlite::memory:'
                    queue: 'test'
        damaged:
            class: PHPQueue\Backend\PDO
            inst-args:
                -
                    connection_string: 'sqlite::memory:'
                    queue: 'damaged'
