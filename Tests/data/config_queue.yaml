default:
    data-store:
        test:
            class: PHPQueue\Backend\PDO
            inst-args:
                -
                    connection_string: 'sqlite::memory:'

        damaged-db:
            class: PDO
            inst-args:
                - 'sqlite::memory:'
